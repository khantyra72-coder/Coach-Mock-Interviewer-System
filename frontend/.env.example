# Copy this file to .env.local only if your backend uses a different port.
# The hostname is selected automatically, including for devices on your LAN.
VITE_API_PORT=8080

# Use this instead only when the API is hosted on a different machine/domain.
# VITE_API_BASE_URL=https://api.example.com/api
